# Frontend environment variables
# Vite exposes variables prefixed with VITE_ to the browser bundle.

# Base URL of the Express backend (only needed if NOT using the Vite proxy)
# With the proxy configured in vite.config.js, /api calls work without this.
VITE_API_URL=http://localhost:5000
